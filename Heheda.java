import java.util.*;
import java.util.stream.Collectors;

public class Heheda {
    public static void main(String[] args) {
        System.out.println((int)(Math.log(3) / Math.log(2)));
        /*
        Set<String> set = new HashSet<>();
        for (int i = 0;i < 1000;++ i) {
            set.add(String.valueOf((int)(Math.random() * 10000)));
        }

        List<String> candidate = new ArrayList<>();
        for (int i = 0;i < 10000;++ i) {
            candidate.add(String.valueOf(i + 1));
        }

        String str = candidate.toString();
        System.out.println(str);

        long start = System.currentTimeMillis();

        String[] splits = str.substring(0, str.length() - 1).substring(1).replaceAll("\\+", "").split(",");
        Set<String> rSet = new HashSet<>(Arrays.asList(splits));

        long end = System.currentTimeMillis();

        System.out.println(end - start);


        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);

        start = System.currentTimeMillis();
        candidate.stream().filter(i -> set.contains(i)).collect(Collectors.toList());
        end = System.currentTimeMillis();
        System.out.println(end - start);*/
    }
}
